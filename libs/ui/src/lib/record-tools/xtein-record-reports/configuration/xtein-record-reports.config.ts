import { InjectionToken } from '@angular/core';

/** Provided by Shell using the existing environment configuration. */
export const XTEIN_REPORTS_URL = new InjectionToken<string>('XTEIN_REPORTS_URL');
