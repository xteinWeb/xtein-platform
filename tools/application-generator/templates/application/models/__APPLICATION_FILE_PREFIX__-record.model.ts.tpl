/**
 * Represents one record from the base table __TABLE_NAME__.
 *
 * Define here every field exposed by the backend for the base table.
 * Backend field names must be preserved when they are part of the
 * application contract.
 */
export class __APPLICATION_CLASS_PREFIX__Record {

  // Define all fields of __TABLE_NAME__ here.

}
